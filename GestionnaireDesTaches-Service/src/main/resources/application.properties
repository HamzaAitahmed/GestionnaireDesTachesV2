spring.application.name=GestionnaireDesTaches-Service
server.port=8088
url.localhost=http://localhost:4200

#spring.main.lazy-initialization=true

#spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true

#spring.datasource.url=jdbc:h2:mem:GDT
#spring.datasource.username=
#spring.h2.console.enabled=true

spring.datasource.url=jdbc:mysql://localhost:3306/GestionnaireDesTacheV2?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.mvc.format.date=yyyy-MM-dd

springdoc.enable-hateoas=false
springdoc.enable-data-rest=false

spring.graphql.graphiql.enabled=true

#spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/ecom-realm
#spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/realms/ecom-realm/protocol/openid-connect/certs

#http://localhost:8088/swagger-ui/index.html
#http://localhost:8088/graphiql

# =======================   Authentification Config  ==============================
cookie.secure=false
cookie.http-only=false
cookie.refresh.path=/api/auth

# 30 jours = 2,592,000,000  | chaque jour = 86,400,000
refreshtoken.time=2592000000

# 15 min   = 900,000        | chaque minute = 60,000
accesstoken.time=900000

jwt.secret=eqaeWSiJbnpDVaBoJFZjDz8qiSGootBl9skZYmKTv2J

# I got a probleme when i set a long secret key, which made the chosen algorithm change between 256, 384, or 512

#               + --------------------- + ---------------- +
#               | Secret size (bits)    | Algorithm chosen |
#               | --------------------- | ---------------- |
#               | ? 256 bits (32 bytes) | HS256            |
#               | ? 384 bits (48 bytes) | HS384            |
#               | ? 512 bits (64 bytes) | HS512            |
#               + --------------------- + ---------------- +