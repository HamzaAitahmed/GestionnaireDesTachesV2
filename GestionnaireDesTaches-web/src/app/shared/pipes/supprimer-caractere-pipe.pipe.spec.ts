import { SupprimerCaracterePipePipe } from './supprimer-caractere-pipe.pipe';

describe('SupprimerCaracterePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SupprimerCaracterePipePipe();
    expect(pipe).toBeTruthy();
  });
});
